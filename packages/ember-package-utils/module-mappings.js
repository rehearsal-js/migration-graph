'use strict';

// this is to support direct imports of module-mappings and should be removed
// once all uses are converted to `const { getModuleMappings } = require('my-package-utils')`
module.exports = require('./index');
